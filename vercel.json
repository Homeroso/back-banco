{
  "version": 2,
  "builds": [
    { "src": "index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.js" }
  ],
  "scripts": {
  "prisma:generate": "prisma generate",
  "vercel-build": "npm run prisma:generate"
}
}
